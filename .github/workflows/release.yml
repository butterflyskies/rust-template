name: Release

on:
  push:
    branches: [main]

# Must queue, not cancel — cancelling mid-run can corrupt the release-please manifest.
concurrency:
  group: release-please
  cancel-in-progress: false

permissions: {}

env:
  REGISTRY: ghcr.io
  # TODO: update to your org/repo name (must match build.yml)
  IMAGE: butterflyskies/my-project

jobs:
  release-please:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
      tag_name: ${{ steps.release.outputs.tag_name }}
    steps:
      - id: release
        uses: googleapis/release-please-action@16a9c90856f42705d54a6fda1823352bdc62cf38 # v4.4.0
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

  # TODO: uncomment ci-gate, publish-image, and publish-binaries when you're ready
  # to ship artifacts on release. The ci-gate job is shared by all publish jobs —
  # it waits for the CI workflow to pass before allowing any artifacts to ship.
  #
  # ci-gate:
  #   needs: release-please
  #   if: ${{ needs.release-please.outputs.release_created == 'true' }}
  #   runs-on: ubuntu-latest
  #   permissions: {}
  #   steps:
  #     - name: Wait for CI to pass
  #       env:
  #         GH_TOKEN: ${{ github.token }}
  #       run: |
  #         echo "Waiting for CI workflow on ${{ github.sha }}..."
  #         for attempt in $(seq 1 20); do
  #           run_id=$(gh run list --repo "${{ github.repository }}" --workflow=build.yml \
  #             --event=push --commit="${{ github.sha }}" --limit=1 --json databaseId -q '.[0].databaseId')
  #           [ -n "$run_id" ] && break
  #           echo "CI run not found yet, retrying in 30s (attempt $attempt/20)..."
  #           sleep 30
  #         done
  #         if [ -z "$run_id" ]; then
  #           echo "::error::CI run not found for ${{ github.sha }} after 10 minutes"
  #           exit 1
  #         fi
  #         echo "Found CI run $run_id, waiting for completion..."
  #         gh run watch "$run_id" --repo "${{ github.repository }}" --exit-status > /dev/null
  #         echo "CI run $run_id passed."
  #
  # publish-image:
  #   needs: [release-please, ci-gate]
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     packages: write
  #     id-token: write
  #   steps:
  #     - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
  #       with:
  #         ref: ${{ needs.release-please.outputs.tag_name }}
  #     - uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0
  #     - uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
  #       with:
  #         registry: ${{ env.REGISTRY }}
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #     - name: Docker meta
  #       id: meta
  #       uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
  #       with:
  #         images: ${{ env.REGISTRY }}/${{ env.IMAGE }}
  #         tags: |
  #           type=semver,pattern={{version}},value=${{ needs.release-please.outputs.tag_name }}
  #           type=semver,pattern={{major}}.{{minor}},value=${{ needs.release-please.outputs.tag_name }}
  #     - uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
  #       with:
  #         context: .
  #         push: true
  #         tags: ${{ steps.meta.outputs.tags }}
  #         labels: ${{ steps.meta.outputs.labels }}
  #         provenance: true
  #         sbom: true
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
  #
  # TODO: update bin name and system dependencies for your project.
  # publish-binaries:
  #   needs: [release-please, ci-gate]
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       include:
  #         - target: x86_64-unknown-linux-gnu
  #           os: ubuntu-latest
  #         # - target: aarch64-apple-darwin
  #         #   os: macos-latest
  #   runs-on: ${{ matrix.os }}
  #   permissions:
  #     contents: write
  #   steps:
  #     - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
  #       with:
  #         ref: ${{ needs.release-please.outputs.tag_name }}
  #     # - name: Install system dependencies
  #     #   if: contains(matrix.target, 'linux')
  #     #   run: sudo apt-get update && sudo apt-get install -y libfoo-dev pkg-config
  #     - uses: dtolnay/rust-toolchain@efa25f7f19611383d5b0ccf2d1c8914531636bf9 # master (2026-02-13)
  #       with:
  #         toolchain: stable
  #         targets: ${{ matrix.target }}
  #     - uses: taiki-e/upload-rust-binary-action@0e34102c043ded9f2ca39f7af5cd99a540c61aff # v1.29.1
  #       with:
  #         bin: my-project
  #         target: ${{ matrix.target }}
  #         archive: my-project-$tag-$target
  #         tar: all
  #         zip: windows
  #         checksum: sha256
  #         token: ${{ secrets.GITHUB_TOKEN }}
  #         ref: refs/tags/${{ needs.release-please.outputs.tag_name }}
